package progetto.AS;
simple AS
{
	parameters:
	    int N;
	    double lamdaS = default(100);
	    double lamdaT = default(10);
	    double mediaS = default(200);
	    
	gates:
		output out;
}